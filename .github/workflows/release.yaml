name: Release

on:
  push:
    tags: ["v*"]

permissions:
  id-token: write
  attestations: write
  contents: write

jobs:
  ci:
    uses: ./.github/workflows/ci.yaml

  build-and-publish:
    needs: ci
    runs-on: ubuntu-latest
    environment: pypi
    steps:
      - uses: step-security/harden-runner@b09bb98e06d4d774595224525879c09bc6e98c40
        with:
          egress-policy: audit

      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1

      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065
        with:
          python-version: "3.11"

      - name: Install build tools
        run: pip install "build==1.5.0" "twine==7.0.0"

      - name: Build package
        run: python -m build

      - name: Verify package
        run: twine check dist/*

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@76f52bc884231f62b54571ab1b768b03ea26f6a8
        with:
          attestations: true

      - name: Attest build provenance
        uses: actions/attest-build-provenance@a2bbfa25375fe432b6a289bc6b6cd05ecc0c4c32
        with:
          subject-path: dist/*

      - name: Create GitHub Release
        uses: softprops/action-gh-release@da05d552573ad5aba039eaac05058a918a7bf631
        with:
          generate_release_notes: true
          files: dist/*
